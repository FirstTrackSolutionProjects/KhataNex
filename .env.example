# URL of the FIRST TRACK KHATANEX backend API.
# Local development (matches the backend's default PORT):
VITE_API_BASE_URL=http://localhost:5001

# In production (Netlify), set this in Site settings -> Environment
# variables to your deployed backend's URL instead, e.g.:
# VITE_API_BASE_URL=https://your-backend.onrender.com
